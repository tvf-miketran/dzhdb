stages:
  - build
  - test
  - deploy

variables:
  IMAGE_TAG: "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA"
  IMAGE_LATEST: "$CI_REGISTRY_IMAGE:latest"
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

default:
  interruptible: true

cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - .cache/pip

build_image:
  stage: build
  image: docker:27
  services:
    - name: docker:27-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - docker build -t "$IMAGE_TAG" -t "$IMAGE_LATEST" .
    - docker push "$IMAGE_TAG"
    - docker push "$IMAGE_LATEST"
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    - if: '$CI_COMMIT_TAG'

test_app:
  stage: test
  image: python:3.12-slim
  before_script:
    - python -V
    - pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    # Keep test command simple and compatible with current repository setup.
    - python -m unittest discover -s . -p "test*.py"
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# ----------------------
# VPS deploy template
# ----------------------
# Enable this job later when you are ready to deploy to VPS.
# Recommended CI/CD variables to add in GitLab:
# - VPS_HOST
# - VPS_USER
# - VPS_PORT (optional, default 22)
# - VPS_PRIVATE_KEY (masked/protected)
# - VPS_DEPLOY_PATH (example: /opt/employee-api)
#
# deploy_vps:
#   stage: deploy
#   image: alpine:3.20
#   before_script:
#     - apk add --no-cache openssh-client
#     - mkdir -p ~/.ssh
#     - echo "$VPS_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_rsa
#     - chmod 600 ~/.ssh/id_rsa
#     - ssh-keyscan -p "${VPS_PORT:-22}" "$VPS_HOST" >> ~/.ssh/known_hosts
#   script:
#     # Option A: pull latest source and run docker compose on VPS
#     - >
#       ssh -p "${VPS_PORT:-22}" "$VPS_USER@$VPS_HOST"
#       "cd $VPS_DEPLOY_PATH &&
#       git pull &&
#       docker compose pull &&
#       docker compose up -d --build"
#   rules:
#     - if: '$CI_COMMIT_BRANCH == "main"'
#       when: manual
#   environment:
#     name: production